apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: helloworld-knative-go
  namespace: default
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: docker.io/scottcranton/helloworld-knative-go
            env:
              - name: TARGET
                value: "Go Sample v1"