apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/scranton/helloworld-knative:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/scranton/helloworld-knative:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: helloworld-knative
        chartPath: charts/helloworld-knative
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/scranton/helloworld-knative'
          image.tag: '{{.DIGEST_HEX}}'
